# API base (production build only — local dev uses Vite proxy to /api)
# VITE_API_URL=https://finpilot-backend.accrescentgroup.com

# Google Sign-In — must match GOOGLE_CLIENT_ID on backend AND authorized origins in Google Cloud Console
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
